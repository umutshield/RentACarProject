Katmanlarý oluþtur. Katmanlara klasörleri ekle. 
Katmanlarda referans vermeyi unutma.(Entities>Core / DataAccess>Entities,Core / Business>DataAccess,Entites / ConsoleUI>core hariç hepsi)

Entityleri oluþtur. (çýplak class kalmasýn)

Service soyut sýnýfý, Manager somut sýnýfý temsil eder. Önce soyut sonra somut sýnýflar yazýlýr.

DataAccess'te entitylerin interfacelerini oluþtur. Burada interfacelere metot yazmayacaðýz generic tipli bir interface(IEntityRepository) 
daha oluþturup buraya metotlarý yazacaðýz ve bu interface'i implement edeceðiz. 

Business'ta entitylerin service interfaclerini(soyut sýnýflarý) oluþtur ve metotlarýný(iþ kurallarýný) yaz.
Business'ta entitylerin managerlerýný(somut sýnýflarý) oluþtur ve interfaceleri implement et. Managerda constructorýný yaz ve bu 
constructorýný soyut nesne ile yaz. Neden soyut bir nesne(ICarDal) ile yazdýk çünkü constructor newlendiðinde bir referans ister eðerki  
biz entityframework  ya da baþka bir oracle ile bu referansý verseydik ve ilerleyen zamanda ef'ü deðiþtirmek isteseydik birçok problem
yaþayacaktýk bu yüzden baðýmlýlýklarý minimize etmek için soyut bir nesneyi yazdýk.
Managerda metotlarýn içeriðini linq ile doldur.

DataAccess'te somut sýnýflarý yani entity frameworkleri oluþtur. EF veritabaný tablolarýyla kodlarýmýz arasýnda iliþki kuran bir üründür.
Entity Framework'ü sisteme ekle. (DataAccess > Nuget)
Ef'ü yükledik ve þimdi kullanmamýz lazým. Veritabaný tablolarýyla kendi classlarýmýzý birbirine baðlamak için DataAccess>EntityFramework'te 
bir class oluþtururuz. (RentacarContext)
DataAccess>EntityFramework'te somut sýnýflara CRUD ve diðer metotlarý yazýp içeriklerini düzenleriz.
Bu somut sýnýflar büyük projelerde çok fazla olacaðý için bir base sýnýfý(EfEntityRepositoryBasse) oluþturup oradan inherit alýrýz.
Entity Framework'ü Core katmaný içinde ekle. (Solution Explorer > Manage Nuget packes for solution)

Ýstersen Business'ta entitylerin soyut ve somut sýnýflarýný oluþtur ve içeriklerini doldur. 

Dto oluþtur.
Dto sql'de yaptýðýmýz tablolardaki bazý satýrlarý birleþtirme iþlemidir. Örneðin arabanýn adýný ve marka adýný yazdýrmak
istiyoruz fakat araba sýnýfýnda marka adý diye bir özellik yok. Ýþte Dto ile bu iki tablodaki satýrlarý birleþtirip yazdýrýrýz.
Entities>DTOs>CarDetailDto / bu sýnýfýn interface'ini yaz interface'in üzerine gel ampülden generate new type de ve 
Core>Entities'e kaydet.
Dto'yu hangi entity için oluþturduysak oranýn soyut sýnýfýnda details metotu yazarýz, entityframeworkünde implement eder ve join iþlemini 
yazarýz.
Sonrasýnda bu metotlarý Business'ta ekle.

